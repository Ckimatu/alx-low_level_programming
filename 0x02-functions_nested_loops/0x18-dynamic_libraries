#include "main.h"

/**
 * add - adds two integers
 * @num1: the first integer added
 * @num2: the second integer added
 *
 * Return: The result of the addition
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
